name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - beautifulsoup4

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov
